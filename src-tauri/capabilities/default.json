{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Detour control panel. Privileged work happens in the helper process, not here, so no extra plugin permissions are needed.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}